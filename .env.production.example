# =============================================================================
# OdinForge Production Environment Variables
# Copy this to /opt/odinforge/.env on your server and fill in values
# =============================================================================

# --- Required ---
POSTGRES_PASSWORD=          # Strong password for Postgres (openssl rand -hex 24)
DATABASE_URL=postgresql://odinforge:YOUR_POSTGRES_PASSWORD@postgres:5432/odinforge
REDIS_URL=redis://redis:6379
SESSION_SECRET=             # Random string (openssl rand -hex 32)
JWT_SECRET=                 # Random string (openssl rand -hex 32)

# --- Domain / URL ---
DOMAIN=                     # e.g. app.odinforge.com (used by Caddy for auto HTTPS)
SERVER_URL=                 # e.g. https://app.odinforge.com (full URL for agent callbacks)

# --- AI / LLM ---
OPENAI_API_KEY=             # OpenAI API key for AI features

# --- S3 / Object Storage (MinIO in docker-compose) ---
MINIO_ROOT_USER=odinforge
MINIO_ROOT_PASSWORD=        # Strong password (openssl rand -hex 24)
STORAGE_ENDPOINT=http://minio:9000
STORAGE_BUCKET_NAME=odinforge
STORAGE_ACCESS_KEY_ID=odinforge
STORAGE_SECRET_ACCESS_KEY=  # Same as MINIO_ROOT_PASSWORD
STORAGE_REGION=us-east-1
STORAGE_FORCE_PATH_STYLE=true

# --- Agent ---
AGENT_REGISTRATION_TOKEN=   # For agent auto-registration (openssl rand -hex 32)
CLOUD_CREDENTIALS_KEY=      # Encryption key for stored cloud creds (openssl rand -hex 32)

# --- Optional ---
# ADMIN_PASSWORD=           # Initial admin password
# PORT=5000                 # App port (default 5000)
# HITL_SIGNING_SECRET=      # For human-in-the-loop approval flows
